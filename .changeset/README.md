# Changesets

This folder holds changeset files created with `bun run changeset`.

Each changeset describes a change that should appear in the changelog and drives the next semver bump when release PRs are merged.
